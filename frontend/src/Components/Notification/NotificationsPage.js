import React from "react";
import NotificationList from "./NotificationList"; // Adjusted path since it's in the same folder

const NotificationsPage = () => {
  return (
    <div>
      <h1>Notifications</h1>
      <NotificationList />
    </div>
  );
};

export default NotificationsPage;
